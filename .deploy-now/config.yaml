# IONOS Deploy Now – root config (required by deploy-to-ionos action)
# Project-specific: .deploy-now/howell-forge-website/config.yaml
version: 1.0
bootstrap:
  excludes: []
recurring:
  excludes: []

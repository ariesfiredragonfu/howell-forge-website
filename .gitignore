# Dependencies and build output
node_modules/

# Env and secrets — never commit
.env
.env.*
.env.local
*.env.local

# GroundChiFlow — build in CI with secrets only; never commit source clone or built bundle
groundchiflow/
groundchiflow-src/

# Common secret patterns (defense in depth)
*.pem
*.key
*.p12
*.jks
credentials.json
*-credentials.json
secrets.json
*.pem
*.key
*.p12
*.jks
credentials.json
*-credentials.json
secrets.json